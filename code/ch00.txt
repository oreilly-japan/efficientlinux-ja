## ======================================================================
$ ls

## ======================================================================
$ paste <(echo {1..10}.jpg | sed 's/ /\n/g') \
<(echo {0..9}.jpg | sed 's/ /\n/g') \
| sed 's/^/mv /' \
| bash

## ======================================================================
$ echo $SHELL
/bin/bash
